import { TelephoneMaskDirective } from './telephone-mask.directive';

describe('TelephoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TelephoneMaskDirective();
    expect(directive).toBeTruthy();
  });
});
